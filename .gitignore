# =============================================
# STM32 + MDK + CubeMX 项目专用 .gitignore
# =============================================

# --- 1. 编译 & 链接生成的临时文件 (必须忽略) ---
*.o
*.d
*.crf
*.lst
*.map
*.axf
*.elf
*.htm
*.dep
*.iex
*.lnp
*.sct
*.build_log.htm

# --- 2. 最终烧录文件 (按需决定) ---
# 如果你不打算在Git里保存固件，就取消下面两行的注释
# *.hex
# *.bin

# --- 3. Keil MDK 个人配置文件 (极易冲突，必须忽略) ---
*.uvoptx          # 用户调试/下载配置 (ST-Link/J-Link等)
*.uvguix.*        # 用户IDE界面布局
*.scvd            # 调试器视图配置
*.dbgconf         # 调试配置文件

# --- 4. CubeMX 生成的备份 & 中间文件 ---
*.mxproject       # CubeMX 项目中间文件
*_CubeMX_*        # CubeMX 可能生成的备份文件
RTE/              # 运行时环境生成的中间文件夹

# --- 5. 调试器日志文件 ---
JLinkLog.txt      # J-Link 调试日志
*.log

# --- 6. 其他常见临时文件 ---
*.bak             # 各种备份文件
*.ini             # 可能包含本地路径的初始化文件
*.iex